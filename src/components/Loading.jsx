const Loading=({center})=>{
    return <div className={center?'loading loading-center':'loading'}></div>
}

export default Loading;

// Checkout https://youtu.be/DqqZEpctZ8w to find how to make the spinner